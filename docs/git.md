# Git Configuration Reference

Source: [`gitconfig`](../gitconfig)

Symlinked to `~/.gitconfig`. Includes `~/.server-configs-generated/gitconfig.compat` for version-adaptive settings (e.g., GitHub CLI credential helper).

---

## User

```
name  = Jianjian Huang
email = jianjian.huang@utah.edu
```

---

## Core Settings

| Setting | Value | Purpose |
|---------|-------|---------|
| `init.defaultBranch` | `main` | Default branch for `git init` |
| `core.editor` | `vim` | Commit message editor |
| `core.autocrlf` | `input` | Convert CRLF → LF on commit, leave LF on checkout |
| `core.pager` | `delta 2>/dev/null \|\| less 2>/dev/null \|\| cat` | Use delta for diffs, fall back to less, then cat |
| `interactive.diffFilter` | `delta --color-only 2>/dev/null \|\| cat` | Delta for interactive diffs |
| `color.ui` | `auto` | Color output when terminal supports it |

---

## Delta (diff pager)

[Delta](https://github.com/dandavison/delta) provides side-by-side diffs with syntax highlighting.

| Setting | Value | Purpose |
|---------|-------|---------|
| `navigate` | `true` | Use `n`/`N` to jump between files in diff |
| `side-by-side` | `true` | Two-column diff layout |
| `line-numbers` | `true` | Show line numbers |

Delta auto-detects the terminal background; no light/dark override is pinned in `gitconfig`.

---

## Behavior

| Setting | Value | Purpose |
|---------|-------|---------|
| `pull.rebase` | `true` | Rebase instead of merge on pull |
| `push.default` | `current` | Push current branch to same-named remote |
| `push.autoSetupRemote` | `true` | Auto-track remote on first push |
| `fetch.prune` | `true` | Delete stale remote-tracking branches |
| `merge.conflictstyle` | `zdiff3` | Three-way merge conflicts with zealous diff (shows base, less noise) |
| `diff.colorMoved` | `default` | Highlight moved lines in diffs |
| `rebase.autoStash` | `true` | Auto-stash before rebase, pop after |
| `rerere.enabled` | `true` | Remember conflict resolutions |

---

## Aliases (`gitconfig`)

| Alias | Expands to | Purpose |
|-------|------------|---------|
| `s` | `status` | Short status |
| `co` | `checkout` | Switch branch |
| `br` | `branch` | List/manage branches |
| `ci` | `commit` | Commit |
| `d` | `diff` | Working tree diff |
| `ds` | `diff --staged` | Staged diff |
| `lg` | `log --oneline --graph --decorate -20` | Compact log (20 entries) |
| `la` | `log --oneline --graph --decorate --all -30` | All-branch log (30 entries) |
| `undo` | `reset --soft HEAD~1` | Undo last commit (keep changes staged) |
| `amend` | `commit --amend --no-edit` | Add to last commit without editing message |
| `stash-all` | `stash save --include-untracked` | Stash everything including new files |
| `wip` | `!git add -A && git commit -m 'WIP'` | Quick work-in-progress commit |

---

## Shell Aliases

See [shell.md](shell.md#git) for the full table. Quick summary:

| Shell alias | Git command |
|-------------|-------------|
| `gs` | `git status -sb` |
| `ga` | `git add` |
| `gc` | `git commit` |
| `gp` | `git push` |
| `gpu` | `git pull` |
| `gl` | `git log --oneline --graph --decorate -20` |
| `gla` | `git log --oneline --all --graph -20` |
| `gd` | `git diff` |
| `gds` | `git diff --staged` |
| `gco` | `git checkout` |
| `gcb` | `git checkout -b` |
| `gb` | `git branch` |
| `lg` | `lazygit` |

---

## Compatibility (`gitconfig.compat`)

Generated by `setup.sh`. May include:

- GitHub CLI credential helper (`gh auth setup-git` equivalent) if `gh` supports it
- SSH-to-HTTPS rewrite for GitHub on hosts without an SSH key (see below)
- Other version-dependent settings

### SSH-to-HTTPS rewrite on keyless hosts

When `~/.ssh/id_{ed25519,rsa,ecdsa,dsa}` is absent — typical after `deploy.sh` bootstraps a remote — `setup.sh` appends a `url.https://github.com/.insteadOf` block covering both `git@github.com:` and `ssh://git@github.com/`. Existing SSH-style remotes then ride the gh HTTPS credential helper instead of failing. On workstations that have a default SSH key the block is skipped, leaving SSH-native flow primary.
